#include <bits/stdc++.h>

using namespace std;

int main()
{
    if (freopen("input.txt", "r", stdin) == nullptr)
    {
        cerr << "Failed to open input file." << std::endl;
        return 1;
    }

    if (freopen("output.txt", "w", stdout) == nullptr)
    {
        cerr << "Failed to open output file." << std::endl;
        return 1;
    }

    string a;
    cin >> a;
    cout << a << "\n";
    cin >> a;
    cout << a << "\n";
}
